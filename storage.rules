rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 公開讀取所有檔案（適合公開網站的靜態圖片）
    match /{allPaths=**} {
      allow read: if true; // 任何人可讀取
      // 僅允許已登入使用者寫入（可依需求調整）
      allow write: if request.auth != null;
    }
  }
}

