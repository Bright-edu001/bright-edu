{
  "hooks": {
    "PostToolUse": [
      {
        "event": "PostToolUse",
        "description": "編輯 src/ 下檔案後，提醒助手確認對應的測試是否需要更新",
        "matcher": {
          "toolName": "edit",
          "filePattern": "src/**/*.jsx,src/**/*.js"
        },
        "steps": [
          {
            "type": "systemMessage",
            "message": "📋 你剛編輯了 src/ 下的檔案。請檢查該檔案是否有對應的 __tests__/ 測試檔案。如果有，請確認修改不會導致測試失敗，或在需要時更新測試。"
          }
        ]
      }
    ]
  }
}
