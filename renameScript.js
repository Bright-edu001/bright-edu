const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(file => { file = path.join(dir, file); const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else if (file.endsWith('.js') && !file.includes('.test.js')) { const content = fs.readFileSync(file, 'utf8'); if (content.includes('import React') || /<[A-Z][a-zA-Z0-9]*|div|span|p|a|ul|li|img/.test(content) && content.includes('/>') || content.includes('className=')) { results.push(file); } } }); return results; } const files = walk('src'); files.forEach(f => { fs.renameSync(f, f.replace('.js', '.jsx')); console.log('Renamed: ' + f); });